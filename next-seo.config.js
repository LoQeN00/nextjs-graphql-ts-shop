const title = 'Sklep Odzieżowy';
const description = 'Tutaj jest opis sklepu';

const nextSeoConfig = {
  title,
  description,
  openGraph: {
    title,
    description,
    site_name: 'SiteName',
  },
};

export default nextSeoConfig;
